// 메인 페이지 인스턴스 생성
const main = new Vue({
    el: '#pageWrap',
    data: {
        islogin: false
    }
})

// 메인페이지 샵박스 드롭다운 버튼

// 메인페이지 샵 박스
const mainShopBox = new Vue({
    el: '#mainShopBox',
    data: {
       
    }
});