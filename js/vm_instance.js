// 메인 페이지 인스턴스 생성
const main = new Vue({
    el: '#pageWrap',
    data: {
        islogin: false
    }
})

// 메인페이지 샵박스 드롭다운 버튼

// 메인페이지 샵 박스
const mainShopBox = new Vue({
    el: '#mainShopBox',
    data: {
       
    }
});

// 디자이너 정보 페이지 샵 박스
const subShopBox = new Vue({
    el: '#sales_tab',
    data: {
       
    }
});

const pfImg = new Vue({
   el: "#pf_tab",
   data: {
       
   }
});